/*
 * 전광판이 바라볼 저장소. GitHub Pages 배포 시 워크플로가 이 파일을
 * 실제 소유자/저장소 이름으로 덮어쓴다.
 */
window.MATDATHON_CONFIG = {
  owner: 'abccba100',
  repo: 'matdathon-judge',
  // 심사 결과를 폴링하는 주기(밀리초).
  refreshMs: 30000,
};
